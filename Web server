A web server is a software or hardware that serves content (like web pages, images, or files) over the internet. 
It responds to requests from web browsers (like Chrome, Firefox) and delivers the requested resources.
